/**
 * SpeedController
 *
 * @description :: Server-side logic for managing Speeds
 * @help        :: See http://sailsjs.org/#!/documentation/concepts/Controllers
 */

var SpeedController = {
	startStream: function(req, res, next) {
		// Opens a stream to grab the request as a JSON object & parse it, then display and push to database
	}
}

module.exports = SpeedController;

